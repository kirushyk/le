project('le', 'c',
    version: '0.0.1',
    meson_version: '>= 0.40.1',
    default_options: [
        'warning_level=1',
        'buildtype=debugoptimized'
    ]
)

cc = meson.get_compiler('c')

subdir('le')
# subdir('tests')
subdir('examples')

